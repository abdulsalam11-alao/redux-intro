import CreateCustomer from "./Featured/Customer/CreateCustomer";
import Customer from "./Featured/Customer/Customer";
import AccountOperations from "./Featured/Account/AccountOperations";
import BalanceDisplay from "./Featured/Account/BalanceDisplay";
import { useSelector } from "react-redux";

function App() {
  const fullName = useSelector((state) => state.customer.fullName);
  return (
    <div>
      <h1>🏦 The React-Redux Bank ⚛️</h1>
      {fullName === "" ? (
        <CreateCustomer />
      ) : (
        <>
          <Customer />
          <AccountOperations />
          <BalanceDisplay />
        </>
      )}
    </div>
  );
}

export default App;
